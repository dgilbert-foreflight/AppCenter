// Generated by Create API
// https://github.com/CreateAPI/CreateAPI

import Foundation
import Get
import HTTPHeaders
import URLQueryEncoder

extension API.Apps.WithOwnerName.WithAppName.Branches.WithBranch {
  public var exportConfig: ExportConfig {
    ExportConfig(path: path + "/export_config")
  }

  public struct ExportConfig {
    /// Path: `/v0.1/apps/{owner_name}/{app_name}/branches/{branch}/export_config`
    public let path: String

    /// Gets the build configuration in Azure pipeline YAML format
    public func get(format: Format? = nil) -> Request<[String: AnyJSON]> {
      Request(method: "GET", url: path, query: makeGetQuery(format), id: "buildConfigurations_get")
    }

    private func makeGetQuery(_ format: Format?) -> [(String, String?)] {
      let encoder = URLQueryEncoder()
      encoder.encode(format, forKey: "format")
      return encoder.items
    }

    public enum Format: String, Codable, CaseIterable {
      case yaml
      case json
    }
  }
}
