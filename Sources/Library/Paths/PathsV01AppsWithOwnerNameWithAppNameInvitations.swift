// Generated by Create API
// https://github.com/CreateAPI/CreateAPI

import Foundation
import Get
import HTTPHeaders
import URLQueryEncoder

extension API.Apps.WithOwnerName.WithAppName {
  public var invitations: Invitations {
    Invitations(path: path + "/invitations")
  }

  public struct Invitations {
    /// Path: `/v0.1/apps/{owner_name}/{app_name}/invitations`
    public let path: String

    /// Gets the pending invitations for the app
    public var get: Request<[String: AnyJSON]> {
      Request(method: "GET", url: path, id: "appInvitations_list")
    }

    /// Invites a new or existing user to an app
    public func post(_ body: PostRequest? = nil) -> Request<Void> {
      Request(method: "POST", url: path, body: body, id: "appInvitations_create")
    }

    public struct PostRequest: Encodable {
      /// The user's email address
      public var userEmail: String
      /// The user's role
      public var role: Role?

      /// The user's role
      public enum Role: String, Codable, CaseIterable {
        case admin
        case collaborator
        case member
      }

      public init(userEmail: String, role: Role? = nil) {
        self.userEmail = userEmail
        self.role = role
      }

      public func encode(to encoder: Encoder) throws {
        var values = encoder.container(keyedBy: StringCodingKey.self)
        try values.encode(userEmail, forKey: "user_email")
        try values.encodeIfPresent(role, forKey: "role")
      }
    }
  }
}
