// Generated by Create API
// https://github.com/CreateAPI/CreateAPI

import Foundation
import Get
import HTTPHeaders
import URLQueryEncoder

extension API.Apps.WithOwnerName.WithAppName.Analytics.Events.WithEventName {
  public var eventCount: EventCount {
    EventCount(path: path + "/event_count")
  }

  public struct EventCount {
    /// Path: `/v0.1/apps/{owner_name}/{app_name}/analytics/events/{event_name}/event_count`
    public let path: String

    /// Count of events by interval in the time range.
    public func get(parameters: GetParameters) -> Request<GetResponse> {
      Request(method: "GET", url: path, query: parameters.asQuery, id: "Analytics_EventCount")
    }

    public struct GetResponse: Decodable {
      public var totalCount: Int64?
      public var previousTotalCount: Int64?
      public var count: [CountItem]?

      public struct CountItem: Decodable {
        /// The ISO 8601 datetime.
        public var datetime: String?
        /// Count of the object.
        public var count: Int64?

        public init(datetime: String? = nil, count: Int64? = nil) {
          self.datetime = datetime
          self.count = count
        }

        public init(from decoder: Decoder) throws {
          let values = try decoder.container(keyedBy: StringCodingKey.self)
          self.datetime = try values.decodeIfPresent(String.self, forKey: "datetime")
          self.count = try values.decodeIfPresent(Int64.self, forKey: "count")
        }
      }

      public init(totalCount: Int64? = nil, previousTotalCount: Int64? = nil, count: [CountItem]? = nil) {
        self.totalCount = totalCount
        self.previousTotalCount = previousTotalCount
        self.count = count
      }

      public init(from decoder: Decoder) throws {
        let values = try decoder.container(keyedBy: StringCodingKey.self)
        self.totalCount = try values.decodeIfPresent(Int64.self, forKey: "total_count")
        self.previousTotalCount = try values.decodeIfPresent(Int64.self, forKey: "previous_total_count")
        self.count = try values.decodeIfPresent([CountItem].self, forKey: "count")
      }
    }

    public struct GetParameters {
      public var start: Date
      public var end: Date?
      public var versions: [String]?

      public init(start: Date, end: Date? = nil, versions: [String]? = nil) {
        self.start = start
        self.end = end
        self.versions = versions
      }

      public var asQuery: [(String, String?)] {
        let encoder = URLQueryEncoder()
        encoder.encode(start, forKey: "start")
        encoder.encode(end, forKey: "end")
        encoder.encode(versions, forKey: "versions", explode: false, delimiter: "|")
        return encoder.items
      }
    }
  }
}
