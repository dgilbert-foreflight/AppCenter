// Generated by Create API
// https://github.com/CreateAPI/CreateAPI

import Foundation
import Get
import HTTPHeaders
import URLQueryEncoder

extension API.Apps.WithOwnerName.WithAppName.Releases.WithReleaseID {
  public var stores: Stores {
    Stores(path: path + "/stores")
  }

  public struct Stores {
    /// Path: `/v0.1/apps/{owner_name}/{app_name}/releases/{release_id}/stores`
    public let path: String

    /// Distributes a release to a store
    public func post(id: UUID) -> Request<PostResponse> {
      Request(method: "POST", url: path, body: ["id": id], id: "releases_addStore")
    }

    public struct PostResponse: Decodable, Identifiable {
      /// Unique id for the release destination
      public var id: String

      public init(id: String) {
        self.id = id
      }

      public init(from decoder: Decoder) throws {
        let values = try decoder.container(keyedBy: StringCodingKey.self)
        self.id = try values.decode(String.self, forKey: "id")
      }
    }
  }
}
