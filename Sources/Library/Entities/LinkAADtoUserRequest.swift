// Generated by Create API
// https://github.com/CreateAPI/CreateAPI

import Foundation

public struct LinkAADtoUserRequest: Codable {
  /// An array of AAD tenant data needed to link the user to the tenants
  public var aadTenantIDs: [UUID]
  /// The role of the user to be added
  public var role: Role?

  /// The role of the user to be added
  public enum Role: String, Codable, CaseIterable {
    case admin
    case collaborator
    case member
  }

  public init(aadTenantIDs: [UUID], role: Role? = nil) {
    self.aadTenantIDs = aadTenantIDs
    self.role = role
  }

  public init(from decoder: Decoder) throws {
    let values = try decoder.container(keyedBy: StringCodingKey.self)
    self.aadTenantIDs = try values.decode([UUID].self, forKey: "aad_tenant_ids")
    self.role = try values.decodeIfPresent(Role.self, forKey: "role")
  }

  public func encode(to encoder: Encoder) throws {
    var values = encoder.container(keyedBy: StringCodingKey.self)
    try values.encode(aadTenantIDs, forKey: "aad_tenant_ids")
    try values.encodeIfPresent(role, forKey: "role")
  }
}
