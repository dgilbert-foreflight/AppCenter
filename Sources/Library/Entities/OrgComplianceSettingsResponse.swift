// Generated by Create API
// https://github.com/CreateAPI/CreateAPI

import Foundation

/// Org settings response
public struct OrgComplianceSettingsResponse: Codable, Identifiable {
  /// The internal unique id (UUID) of the organization compliance setting
  public var id: String
  /// The internal unique id (UUID) of the organization.
  public var orgID: String
  /// Certificate connection id to wrap and resign the app after wrapping
  public var certificateConnectionID: String
  /// Flag to tell if mam warpping is enabled on the Org
  public var isMamEnabled: Bool?

  public init(id: String, orgID: String, certificateConnectionID: String, isMamEnabled: Bool? = nil) {
    self.id = id
    self.orgID = orgID
    self.certificateConnectionID = certificateConnectionID
    self.isMamEnabled = isMamEnabled
  }

  public init(from decoder: Decoder) throws {
    let values = try decoder.container(keyedBy: StringCodingKey.self)
    self.id = try values.decode(String.self, forKey: "id")
    self.orgID = try values.decode(String.self, forKey: "org_id")
    self.certificateConnectionID = try values.decode(String.self, forKey: "certificate_connection_id")
    self.isMamEnabled = try values.decodeIfPresent(Bool.self, forKey: "is_mam_enabled")
  }

  public func encode(to encoder: Encoder) throws {
    var values = encoder.container(keyedBy: StringCodingKey.self)
    try values.encode(id, forKey: "id")
    try values.encode(orgID, forKey: "org_id")
    try values.encode(certificateConnectionID, forKey: "certificate_connection_id")
    try values.encodeIfPresent(isMamEnabled, forKey: "is_mam_enabled")
  }
}
