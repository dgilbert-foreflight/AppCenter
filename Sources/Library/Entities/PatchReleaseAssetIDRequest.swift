// Generated by Create API
// https://github.com/CreateAPI/CreateAPI

import Foundation

/// Patch the asset id of a release request body
public struct PatchReleaseAssetIDRequest: Codable {
  /// The release new package id in ACFUS
  public var packageAssetID: UUID
  /// The release upload id used to upload the release
  public var uploadID: UUID
  /// The ipa UUIDs for this release, as a serialized JSON array
  public var ipaUUIDs: String?

  public init(packageAssetID: UUID, uploadID: UUID, ipaUUIDs: String? = nil) {
    self.packageAssetID = packageAssetID
    self.uploadID = uploadID
    self.ipaUUIDs = ipaUUIDs
  }

  public init(from decoder: Decoder) throws {
    let values = try decoder.container(keyedBy: StringCodingKey.self)
    self.packageAssetID = try values.decode(UUID.self, forKey: "package_asset_id")
    self.uploadID = try values.decode(UUID.self, forKey: "upload_id")
    self.ipaUUIDs = try values.decodeIfPresent(String.self, forKey: "ipa_uuids")
  }

  public func encode(to encoder: Encoder) throws {
    var values = encoder.container(keyedBy: StringCodingKey.self)
    try values.encode(packageAssetID, forKey: "package_asset_id")
    try values.encode(uploadID, forKey: "upload_id")
    try values.encodeIfPresent(ipaUUIDs, forKey: "ipa_uuids")
  }
}
