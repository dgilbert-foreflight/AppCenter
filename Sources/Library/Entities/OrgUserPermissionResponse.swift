// Generated by Create API
// https://github.com/CreateAPI/CreateAPI

import Foundation

public struct OrgUserPermissionResponse: Codable {
  /// The unique id (UUID) of the org
  public var orgID: UUID
  /// The user role for the org
  public var userRole: UserRole

  /// The user role for the org
  public enum UserRole: String, Codable, CaseIterable {
    case admin
    case collaborator
  }

  public init(orgID: UUID, userRole: UserRole) {
    self.orgID = orgID
    self.userRole = userRole
  }

  public init(from decoder: Decoder) throws {
    let values = try decoder.container(keyedBy: StringCodingKey.self)
    self.orgID = try values.decode(UUID.self, forKey: "orgId")
    self.userRole = try values.decode(UserRole.self, forKey: "userRole")
  }

  public func encode(to encoder: Encoder) throws {
    var values = encoder.container(keyedBy: StringCodingKey.self)
    try values.encode(orgID, forKey: "orgId")
    try values.encode(userRole, forKey: "userRole")
  }
}
